// $(function () {
//     $('#fileupload').fileupload({
//         dataType: 'json',
//         done: function (e, data) {
//             $.each(data.result.files, function (index, file) {
//                 $('<p/>').text(file.name).appendTo(document.body);
//             });
//         }
//     });
// });

// $(function() {
//   if($("#new_asset").size() == 1 ){
//     $("#new_asset").fileupload();

//     $.getJSON($('#new_asset').prop('action'), function (files) {
//       var fu = $('#new_asset').data('fileupload'), template;

//       fu._adjustMaxNumberOfFiles(-files.length);
//       template = fu._renderDownload(files).appendTo($('#new_asset .files'));

//       fu._reflow = fu._transition && template.length && template[0].offsetWidth;
//       template.addClass('in');
//       $('#loading').remove();
//     });
//   }
// });
// $(function() {
//   if($("#new_image").size() == 1 ){
//     $("#new_image").fileupload();

//     $.getJSON($('#new_image').prop('action'), function (files) {
//       var fu = $('#new_image').data('fileupload'), template;

//       fu._adjustMaxNumberOfFiles(-files.length);
//       template = fu._renderDownload(files).appendTo($('#new_image .files'));

//       fu._reflow = fu._transition && template.length && template[0].offsetWidth;
//       template.addClass('in');
//       $('#loading').remove();
//     });
//   }
// });
