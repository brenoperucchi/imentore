require 'spec_helper'

describe "Stores" do
  context "#new" do
    it "renders the form to create a new store" do
      # visit '/stores/new'
      # page.should have_selector('form')
      # page.should have_selector('#imentore_store_url')
      # page.should have_selector('#imentore_store_owner_user_email')
    end
  end
end