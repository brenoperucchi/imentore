p List Domains

ul
- @domains.each do |domain|
  li
    = domain.name
    = " => " + domain.hosting.to_s
    = button_to("Destroy", admin_domain_path(domain), method: :delete)
    = link_to "Mails", emails_admin_domain_path(domain)

p Create Domain

= simple_form_for([:admin, @domain]) do |f|
  = f.input :name
  = f.check_box :hosting
  = f.submit